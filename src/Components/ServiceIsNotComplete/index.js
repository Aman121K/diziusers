import React from "react";
import { View } from 'react-native';
const ServiceIsNotComplete = ({ }) => {
    return (
        <View>
            <Text>Service is not complete</Text>
        </View>
    )
}
export default ServiceIsNotComplete;