import React from 'react';
import { StyleSheet, TouchableOpacity, View } from 'react-native';
const style = StyleSheet.create({
    mainContainer: {

    }
})
const CustomerList = ({ item, navigation }) => {
    console.log("item>>", item)
    return (
        <TouchableOpacity>


        </TouchableOpacity>
    )
}
export default CustomerList;